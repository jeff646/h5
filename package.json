{
  "name": "h5-project",
  "version": "1.0.0",
  "description": "H5 project description",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && npm run compile",
    "clean": "rimraf dist",
    "compile": "webpack --config webpack.config.js"
  },
  "dependencies": {
    "some-package": "^1.0.0"
  },
  "devDependencies": {
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0",
    "rimraf": "^3.0.0"
  }
}
